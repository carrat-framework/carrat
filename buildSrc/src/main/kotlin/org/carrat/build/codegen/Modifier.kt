package org.carrat.build.codegen

enum class Modifier(val string : String) {
    PUBLIC("public"),
    PRIVATE("private"),
    ABSTRACT("abstract"),
    INTERNAL("internal"),
    OVERRIDE("override")
}